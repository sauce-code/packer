<?xml version="1.0" encoding="UTF-8"?>

<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns="http://www.saucecode.com/packer/xml"
	targetNamespace="http://www.saucecode.com/packer/xml"
	elementFormDefault="qualified">

	<xs:element name="name" type="xs:string" />
	
	<xs:element name="category" type="xs:string" />
	
	<xs:element name="set" type="xs:string" />

	<xs:element name="sets">
		<xs:complexType>
			<xs:sequence>
				<xs:element ref="set" maxOccurs="unbounded" />
			</xs:sequence>
		</xs:complexType>
	</xs:element>

	<xs:element name="item">
		<xs:complexType>
			<xs:sequence>
				<xs:element ref="name" />
				<xs:element ref="category" />
				<xs:element ref="sets" />
			</xs:sequence>
		</xs:complexType>
	</xs:element>

	<xs:element name="items">
		<xs:complexType>
			<xs:sequence>
				<xs:element ref="item" maxOccurs="unbounded" />
			</xs:sequence>
		</xs:complexType>
	</xs:element>

</xs:schema>